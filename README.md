#This is a PRIVATE project and I will give information just for who has permission.

After this, if you run the git --version command in Terminal, it should display the version of Git that you just installed. 
